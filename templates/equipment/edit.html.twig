{% extends 'base.html.twig' %}

{% block title %}Editer l'équipement: {{ equipment.name}}{% endblock %}

{% block body %}
    <h3>Editer l'équipement: <em>{{ equipment.name}} | {{ equipment.number }}</em></h3>

    {{ include('equipment/_form.html.twig', {'button_label': 'Modifier'}) }}

    {{ include('equipment/_delete_form.html.twig') }}
{% endblock %}
